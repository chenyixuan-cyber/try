{
  // -------------------- 输出相关 --------------------
  "output_prefix": "./src/bringup/map/map",  
  // 作用：输出文件前缀，最终生成 map.pgm 和 map.yaml

  // -------------------- 地图分辨率 --------------------
  "resolution": 0.05,      
  // 作用：地图像素大小（米/像素），必须，有效
  // 分辨率越小，地图越精细，但文件大、计算慢

  // -------------------- 高度过滤 --------------------
  "min_z_height": -0.05,   
  "max_z_height": 1.0,     
  // 作用：点云 Z 过滤区间
  // 只有 Z 在这个区间的点会参与生成地图
  // 核心参数，必用

  // -------------------- 体素下采样 --------------------
  "voxel_leaf_size": 0.1,  
  // 作用：点云体素滤波大小
  // 下采样点云，减少点数，提高效率
  // 必用，但可以根据雷达点密度调整

  // -------------------- 栅格像素值 --------------------
  "occupied_value": 0,      
  "free_value": 255,        
  "unknown_value": 205,     
  // 作用：
  // occupied_value → 障碍物像素值（黑）
  // free_value → 空闲像素值（白）
  // unknown_value → 未观测像素值（灰）

  // -------------------- 地图边缘扩展 --------------------
  "map_padding": 1.0,      
  // 作用：在点云边界外扩展地图尺寸（米）
  // 推荐保留，防止规划器贴边

  // -------------------- 雷达旋转（可选） --------------------
  "lidar_roll_deg": -45.0,
  "lidar_pitch_deg": 0.0,
  "lidar_yaw_deg": 0.0
  // 作用：旋转点云到水平（目前你的代码里用）
  // 如果你把旋转放进 TF，这些参数可以删
}
